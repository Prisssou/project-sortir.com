<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    {# Mise en place de l'icone de l'onglet #}
    <link rel="icon" href="{{ asset('img/favicon/point_of_interest.png') }}"/>
    {# Mise en place du fichier CSS #}
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    {# Mise en place de de Bootstrap #}
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
          integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>{% block title %} Sortir.com - Votre site de sortie {% endblock %}</title>


{#    Import of specific css files#}
    {% block stylesheet %}{% endblock %}

</head>
<body>

{% include 'includes/nav.html.twig' %}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div id="alert" class="alert-{{ label }}">{{ message }}</div>
    {% endfor %}
{% endfor %}



{% block body %}
    Home
{% endblock %}

</body>
</html>